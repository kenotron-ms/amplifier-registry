screenshot*.png
.superpowers/
.DS_Store

# CI script dependencies — never commit, not part of the published site
.github/scripts/node_modules/
.github/scripts/package-lock.json

# HuggingFace model weight cache (used by generate-embeddings.mjs)
.hf-cache/
